{
  "name": "personal-website-audit",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "description": "Self-contained browser measurement harness for the site (Playwright + Lighthouse + axe-core). Not part of the main app build.",
  "scripts": {
    "measure": "node measure.mjs"
  },
  "dependencies": {
    "@axe-core/playwright": "^4.10.1",
    "lighthouse": "^12.6.0",
    "playwright": "^1.55.0"
  }
}
